[package]
name = "gpx_processor"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
warp = "0.3"
gpx = "0.10"
tokio = { version = "1", features = ["full"] }

# spatial index and lightweight on-disk cache
rstar = "0.11"
sled = "0.34"